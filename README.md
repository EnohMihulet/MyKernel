MyKernel

A bare-metal AArch64 kernel built from scratch, running on QEMU’s virt machine. So far, there is some entry asm code, allowing the transition into C. Then UART initialization allowing for printing to screen.
